var Promise = require("bluebird");
var StockEntry = require('../models/StockEntry');

var StockService = function () {

  var queue = [];

  this.runStockOperation = function (stockOperationFn) {
       
  };

};
